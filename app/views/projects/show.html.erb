<h1><%= @project.name %></h1>
<h2><%= @partner.name %></h2>

<div>
  <p>Help heal the bay today!</p>
  <div>
<!--   <a id="cta005" href="receipt.html" class="tbn btn-success btn-lg">
    <span class="glyphicon glyphicon-ok"></span>$5 Donation</a> -->

    <%= form_tag charges_path do %>
      <article>
        <% if flash[:error].present? %>
          <div id="error_explanation">
            <p><%= flash[:error] %></p>
          </div>
        <% end %>
        <label class="amount">
          <span>Amount: $5.00</span>
        </label>
      </article>

      <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
              data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
              data-amount="500"
              data-name="Climate Cents"
              data-description="<%= @project.name %>"
              data-locale="auto"
              data-zip-code="true">>
              </script>
    <% end %>

  </div>
</div>

<hr/>
<%= link_to 'Back', :back, class: "btn btn-primary btn-sm" %>
